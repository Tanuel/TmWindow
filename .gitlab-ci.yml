image: node:latest

pages:
  stage: deploy

  cache:
    paths:
      - node_modules/

  script:
    - npm prune
    - npm install -g yarn
    - yarn install
    - yarn build-docs

  artifacts:
    paths:
      - public

  only:
    - master