image: node:latest

stages:
  - test
  - release
  - deploy

cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - node_modules/

test:lint:
  stage: test
  script:
    - npm prune
    - npm install -g yarn
    - yarn install
    - yarn lint
  except:
    - tags

publish:
  stage: release
  cache:
    policy: pull
  script:
    - npm prune
    - npm install -g yarn
    - yarn install
    - yarn build
    - yarn build:dist
    - yarn release
  only:
    - master

pages:
  stage: deploy
  cache:
    policy: pull
  script:
    - npm prune
    - npm install -g yarn
    - yarn install
    - yarn build:docs
    - yarn build:typedoc
  artifacts:
    paths:
      - public
  only:
    - master