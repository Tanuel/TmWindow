image: node:latest

cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - node_modules/

pages:
  stage: deploy
  script:
    - npm prune
    - npm install -g yarn
    - yarn install
    - yarn docs
  artifacts:
    paths:
      - public
  only:
    - master