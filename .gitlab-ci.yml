image: node:latest

stages:
  - test
  - deploy

cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - node_modules/

test:lint:
  stage: test
  script:
    - npm prune
    - npm install -g yarn
    - yarn install
    - yarn lint
  except:
    - tags

publish:
  stage: deploy
  cache:
    policy: pull
  script:
    - npm prune
    - npm install -g yarn
    - yarn install
    - yarn build
    - yarn release
  artifacts:
    paths:
      - public
  only:
    - master