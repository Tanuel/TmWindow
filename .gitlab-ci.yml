image: node:latest

pages:
  stage: deploy

  cache:
    paths:
      - node_modules/

  script:
    - npm prune
    - npm install -g yarn grunt-cli
    - yarn install
    - grunt build

  artifacts:
    paths:
      - public

  only:
    - master